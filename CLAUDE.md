# CLAUDE.md

@AGENTS.md

## Claude Code 专属补充

上面那行 `@AGENTS.md` 是 Claude Code 的 import 语法——通用约定（架构、命令、安全模型、
契约、Git 工作流、验收要求）全部在 `AGENTS.md` 里，本文件只放 Claude 才能用的东西。

（当前没有 Claude 专属内容。要加的话，例如：只对某些目录用 plan mode、本项目自定义
Skill 的名字与用途等。**通用规则别写在这里**——Codex/opencode/pi 读不到本文件。）
